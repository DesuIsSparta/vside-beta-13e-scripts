function writeOutFunctions()
{
    new ConsoleLogger(Logger);
    dumpConsoleFunctions();
    Logger.delete();
}
function writeOutClasses()
{
    new ConsoleLogger(Logger);
    dumpConsoleClasses();
    Logger.delete();
}
